import { Files, PI } from './inside/files';

class Computer {
    private files: Files;

    constructor() {
        console.log(PI);
    }

    reSizeIt(sz?: number) {

    }
}

let com : Computer = new Computer();
com.reSizeIt();